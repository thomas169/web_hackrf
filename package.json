{
  "name": "web-hackrf",
  "version": "1.0.0",
  "description": "Browser-based SDR tools for HackRF and RTL-SDR",

  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/w3c-web-usb": "^1.0.10"
  }
}